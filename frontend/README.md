# RAG 知识库问答 - 前端

基于 **Vue 3 + Vite** 的 RAG 问答系统前端界面。

## 启动

```bash
npm install
npm run dev
```

默认运行在 `http://localhost:5173`。

## 页面

- **💬 问答** — 气泡对话界面，支持来源标注
- **📁 文档管理** — 上传/删除文档

## 后端地址

默认请求 `http://localhost:8000`，可在 `src/api.js` 中修改 `BASE` 常量。
